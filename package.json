{
  "name": "speaker-deck-worker",
  "version": "1.0.0",
  "description": "Notion Worker: fires the Claude deck-creation routine immediately when a deck task is created",
  "main": "worker.ts",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "@notionhq/workers": "latest"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^5.9.3"
  }
}
